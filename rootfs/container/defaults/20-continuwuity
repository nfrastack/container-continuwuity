#!/command/with-contenv bash
# SPDX-FileCopyrightText: © 2026 Nfrastack <code@nfrastack.com>
#
# SPDX-License-Identifier: MIT

SETUP_TYPE=${SETUP_TYPE:-"AUTO"}

SERVER_NAME=${SERVER_NAME:-${CONTINUWUITY_SERVER_NAME:-}}
DATA_PATH=${DATA_PATH:-${CONTINUWUITY_DATABASE_PATH:-"/data/"}}
CONFIG_PATH=${CONFIG_PATH:-"/config/"}
CONFIG_FILE=${CONFIG_FILE:-"conduwuit.toml"}

LISTEN_PORT=${LISTEN_PORT:-${CONTINUWUITY_PORT:-"8008"}}
LISTEN_IP=${LISTEN_IP:-${CONTINUWUITY_ADDRESS:-"0.0.0.0"}}

LOG_TYPE=${LOG_TYPE:-"both"}
LOG_LEVEL=${LOG_LEVEL:-${CONTINUWUITY_LOG_LEVEL:-"info"}}
LOG_PATH=${LOG_PATH:-"/logs/continuwuity/"}
LOG_FILE=${LOG_FILE:-"continuwuity.log"}

CONTINUWUITY_NEW_USER_DISPLAYNAME_SUFFIX=${CONTINUWUITY_NEW_USER_DISPLAYNAME_SUFFIX:-""}

ALLOW_REGISTRATION=${ALLOW_REGISTRATION:-${CONTINUWUITY_ALLOW_REGISTRATION:-"false"}}
ALLOW_FEDERATION=${ALLOW_FEDERATION:-${CONTINUWUITY_ALLOW_FEDERATION:-"true"}}
ALLOW_ENCRYPTION=${ALLOW_ENCRYPTION:-${CONTINUWUITY_ALLOW_ENCRYPTION:-"true"}}
ALLOW_ROOM_CREATION=${ALLOW_ROOM_CREATION:-${CONTINUWUITY_ALLOW_ROOM_CREATION:-"true"}}
get_defaults_advanced
